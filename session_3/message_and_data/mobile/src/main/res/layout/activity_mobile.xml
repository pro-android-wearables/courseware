<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MobileActivity">

    <EditText
        android:id="@+id/message_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:hint="@string/enter_message"
        android:lines="1"/>

    <ImageButton
        android:id="@+id/add_button"
        android:layout_width="@dimen/button_diameter"
        android:layout_height="@dimen/button_diameter"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="@dimen/buttom_margin"
        android:layout_marginEnd="@dimen/buttom_margin"
        android:background="@drawable/oval_ripple"
        android:elevation="@dimen/elevation_low"
        android:onClick="sendMessageToWear"
        android:src="@android:drawable/ic_menu_send"
        android:stateListAnimator="@anim/button_elevation"
        android:tint="@android:color/white"/>

    <ImageButton
        android:id="@+id/camera_button"
        android:layout_width="@dimen/button_diameter"
        android:layout_height="@dimen/button_diameter"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="@dimen/buttom_margin"
        android:layout_marginStart="@dimen/buttom_margin"
        android:background="@drawable/oval_ripple"
        android:elevation="@dimen/elevation_low"
        android:onClick="sendPhotoToWear"
        android:src="@android:drawable/ic_menu_camera"
        android:stateListAnimator="@anim/button_elevation"
        android:tint="@android:color/white"/>

</RelativeLayout>
